define(function(require) {
  var _ = require('underscore');

  var TileBuilder = function(x, y) {
    this.x = x;
    this.y = y;
  };

  TileBuilder.prototype = {
    addStatic: function() {

    },
    addEntity: function() {

    } 
  };

  return TileBuilder;
});